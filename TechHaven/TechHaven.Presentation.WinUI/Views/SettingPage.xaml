<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TechHaven.Presentation.WinUI.Views.SettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TechHaven.Presentation.WinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <StackPanel Spacing="18">
            <TextBlock Text="Application Settings" FontSize="20" FontWeight="SemiBold" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="12">
                <TextBlock Text="Color mode" VerticalAlignment="Center" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>

                <Button x:Name="themeButton" Click="ThemeButton_Click" Margin="8,0,0,0"/>
            </StackPanel>

            <TextBlock x:Name="themeStatusText" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>

            <!-- Preview panel to visualize theme changes locally -->
            <Border x:Name="previewPanel" CornerRadius="8" Padding="12" BorderThickness="1" BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" Margin="0,12,0,0" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}">
                <StackPanel Spacing="8">
                    <TextBlock Text="Preview" FontWeight="SemiBold" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
                    <TextBlock Text="This area shows how controls look under the selected theme." TextWrapping="Wrap" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
                    <Button Content="Sample Button" Width="140" Background="{ThemeResource SystemControlBackgroundAccentBrush}" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
                    <TextBox PlaceholderText="Sample input" Width="220" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>
